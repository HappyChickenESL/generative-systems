# Generative Systems

## Setup

1. `git clone https://github.com/HappyChickenESL/generative-systems.git`
2. `npm i`
3. `npm run dev`

## Issues

I have implemented Lazy Loading for all routes, so unneccessary objects are not rendered immediately. If your client begins to lag, consider refreshing your browser :)
